# คู่มือติดตั้ง E-Learning (กดทีละปุ่ม)

โฟลเดอร์นี้คือทั้งเว็บ ไม่มีการติดตั้งเครื่องมือ ไม่มีการพิมพ์คำสั่ง คุณแค่ทำ 4 ส่วนนี้ตามลำดับ

ไฟล์ในโฟลเดอร์:
- `index.html`, `styles.css`, `app.js`, `config.js` = ตัวเว็บ (Vercel เอาขึ้นเว็บให้)
- `supabase/migrations/0001_init.sql`, `0002_seed.sql` = ตัวสร้างฐานข้อมูล (เอาไปวางใน Supabase)

---

## ส่วนที่ 1 — สร้างฐานข้อมูลใน Supabase

**สาเหตุ:** เว็บต้องมีที่เก็บข้อมูล (ผู้ใช้ บทเรียน ข้อสอบ ผลสอบ) เราจะสร้างในโปรเจกต์ Supabase ใหม่ ไม่ไปแตะโปรเจกต์เดิมของบริษัท

**ผมทำให้แล้ว:** เขียนไฟล์ฐานข้อมูล 2 ไฟล์ (`0001_init.sql` สร้างห้องเก็บข้อมูลแบบล็อกไว้ + ประตูที่ปลอดภัย, `0002_seed.sql` ใส่บัญชีแอดมิน 1 คนกับบทเรียน/ข้อสอบตัวอย่าง)

**คุณต้องทำ:**
1. เปิดเว็บ supabase.com แล้วเข้าโปรเจกต์ใหม่ที่ตั้งไว้ (URL ลงท้าย `...botuwnueqgihugnwongf`)
2. เมนูซ้ายมือ กด **SQL Editor**
3. กดปุ่ม **+ New query** (มุมบนซ้ายของกล่อง)
4. เปิดไฟล์ `supabase/migrations/0001_init.sql` ด้วย Notepad/TextEdit → เลือกทั้งหมด (Ctrl/Cmd + A) → คัดลอก (Ctrl/Cmd + C)
5. กลับมาที่ Supabase → คลิกในกล่องว่าง → วาง (Ctrl/Cmd + V)
6. กดปุ่ม **Run** (มุมขวาล่าง)
   - **สำเร็จ:** ขึ้นข้อความเขียว "Success. No rows returned"
7. กด **+ New query** อีกครั้ง แล้วทำข้อ 4–6 ซ้ำกับไฟล์ `0002_seed.sql`
   - **สำเร็จ:** ขึ้นเขียว "Success" เหมือนเดิม

> สำคัญ: ห้ามเข้าเมนู Table Editor ไปแก้ตารางด้วยมือ ถ้าต้องเปลี่ยนโครงสร้าง ให้ผมทำไฟล์ `0003_...sql` ใหม่ให้แล้วคุณเอามาวาง Run แบบเดิม

---

## ส่วนที่ 2 — เอาโค้ดขึ้น GitHub (ใช้ GitHub Desktop)

**สาเหตุ:** Vercel จะดึงเว็บจาก GitHub ทุกครั้งที่มีการอัปเดต เราจึงเก็บไฟล์ไว้บน GitHub ก่อน

**ผมทำให้แล้ว:** จัดไฟล์เว็บทั้งหมดไว้ในโฟลเดอร์ `elearning-web` พร้อมอัปโหลด

**คุณต้องทำ:**
1. เอาโฟลเดอร์ `elearning-web` ไปวางไว้ที่ **Desktop** ของคุณ
2. เปิดโปรแกรม **GitHub Desktop**
3. เมนูบนซ้าย **File → New repository...** ไม่ใช่ — ให้ใช้ **File → Add local repository...**
4. กด **Choose...** แล้วเลือกโฟลเดอร์ `elearning-web` บน Desktop → กด **Add repository**
   - ถ้าขึ้นข้อความว่า "This directory does not appear to be a Git repository" ให้กดลิงก์ **create a repository** ที่อยู่ในข้อความนั้น แล้วกด **Create repository**
5. ล่างซ้าย ช่อง Summary พิมพ์ว่า `first version` → กดปุ่มฟ้า **Commit to main**
6. บนขวา กดปุ่ม **Publish repository**
7. ในหน้าต่างที่เด้งขึ้น: ช่อง Name พิมพ์ `elearning-web` → **เอาเครื่องหมายถูกออกจากช่อง "Keep this code private" ก็ได้ หรือคงไว้ก็ได้** → กด **Publish repository**
   - **สำเร็จ:** ปุ่มบนขวาเปลี่ยนเป็น **Fetch origin** แปลว่าโค้ดขึ้น GitHub แล้ว

> ต่อไปเวลาผมแก้ไฟล์ให้ คุณแค่เปิด GitHub Desktop → พิมพ์ข้อความสั้น ๆ ในช่อง Summary → **Commit to main** → **Push origin** เท่านั้น เว็บจะอัปเดตเอง

---

## ส่วนที่ 3 — เอาเว็บขึ้นออนไลน์ (Vercel)

**สาเหตุ:** Vercel คือที่ที่ทำให้เว็บเปิดได้จากอินเทอร์เน็ตจริง

**ผมทำให้แล้ว:** เว็บนี้เป็นไฟล์ล้วน ๆ ไม่ต้องตั้งค่าพิเศษ Vercel เปิดได้ทันที

**คุณต้องทำ:**
1. เปิดเว็บ vercel.com แล้วล็อกอินด้วยบัญชีที่เตรียมไว้
2. กดปุ่ม **Add New...** (มุมบนขวา) → เลือก **Project**
3. หา `elearning-web` ในรายการ แล้วกด **Import**
4. หน้าตั้งค่าที่เด้งมา **ไม่ต้องแก้อะไรเลย** กดปุ่ม **Deploy** ได้เลย
5. รอสักครู่จนขึ้นรูปพลุ/ข้อความ **Congratulations**
6. กดปุ่ม **Continue to Dashboard** แล้วกดลิงก์ที่ลงท้าย `.vercel.app` เพื่อเปิดเว็บ
   - **สำเร็จ:** เห็นหน้าล็อกอิน E-Learning Platform

---

## ส่วนที่ 4 — ทดสอบเข้าระบบ

**คุณต้องทำ:**
1. ที่หน้าล็อกอิน ช่องอีเมลพิมพ์ `sumaitri.nu@digiservecorp.com`
2. ช่องรหัสผ่านพิมพ์ `Change1234!`
3. กด **เข้าสู่ระบบ**
   - **สำเร็จ:** เข้าหน้าหลักได้ เห็นเมนู หน้าหลัก / บทเรียน / แบบทดสอบ / จัดการผู้ใช้
4. ลองกด **แบบทดสอบ** → เข้าชุดตัวอย่าง → ทำแล้วส่ง ต้องเห็นคะแนนและเฉลย

> รหัส `Change1234!` เป็นรหัสตั้งต้น เดี๋ยวเฟสถัดไปผมจะทำหน้าเปลี่ยนรหัสให้

---

## ต่อไป (ผมจะทำให้เมื่อคุณพร้อม)
- ย้ายข้อมูลเดิมจาก Google Sheet (ผู้ใช้ / ข้อสอบ / ผลสอบ / บทเรียน) เข้ามา
- ผูกโดเมน `e-learning.digiservecorp.com` ที่ Vercel
- หน้าแดชบอร์ดผลสอบ + ใบประกาศ + มอบหมายงาน
